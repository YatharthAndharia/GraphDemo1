{
  "name": "Test SubGraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ Test SubGraph",
    "create-local": "graph create --node http://localhost:8020/ Test SubGraph",
    "remove-local": "graph remove --node http://localhost:8020/ Test SubGraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 Test SubGraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
